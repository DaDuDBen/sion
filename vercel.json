{
  "installCommand": "cd client && npm ci",
  "buildCommand": "cd client && npm run build",
  "outputDirectory": "client/dist",
  "functions": {
    "api/**/*.py": {
      "runtime": "python3.12"
    }
  }
}
